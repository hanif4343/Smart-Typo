{
  "name": "typebangla",
  "version": "0.1.0",
  "private": true,
  "description": "AI Typing RPG for Bangla — web + Android via Capacitor",
  "scripts": {
    "cap:add-android": "cap add android",
    "cap:sync": "cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
